import React from 'react';

const Filters = () => {
    return (
        <div className='filters'>
            <button>All</button>
            <button>Done</button>
            <button>Todo</button>
        </div>
    );
}

export default Filters;
